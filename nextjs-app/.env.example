# AQla v2 staging is AWS-first.
# The Cognito client secret is stored in AWS Secrets Manager, not in this file.
# Runtime AWS permissions are supplied through the Amplify SSR Compute role.

# Future non-secret/runtime configuration may be added here when needed.
SES_FROM_EMAIL=
OPENAI_API_KEY=
